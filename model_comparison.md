# 임베딩 모델 비교 (1GB RAM 제약 고려)

## 후보 모델 분석

### 1. **sentence-transformers/ko-sbert-multitask** ⭐ 추천
- **장점**:
  - 한국어 자동 평가 (KLUE, KorSTS) 상위권
  - 모델 크기: ~500MB (압축 형태)
  - 입출력 차원: 384 (가볍다)
  - 멀티태스크 학습으로 일반화 성능 우수
  - huggingface로 쉽게 다운로드
- **단점**: 
  - 없음
- **추정 메모리**: ~1-1.5GB (로드 시)
- **추천도**: ⭐⭐⭐⭐⭐

### 2. sentence-transformers/LaBSE
- **장점**:
  - 다국어 지원 (한국어 포함)
  - 크로스랭귀지 검색 가능
- **단점**:
  - 모델 크기: ~2GB (너무 크다)
  - 1GB 서버에서는 위험
- **추정 메모리**: ~2-2.5GB
- **추천도**: ❌ (1GB 서버에는 부담)

### 3. KoSimCSE (코사인 유사도 기반)
- **장점**:
  - 매우 가벼움 (~300MB)
  - 법률/규정 텍스트 성능 검증 필요
- **단점**:
  - 한국 커뮤니티 모델 (업데이트 빈도 낮음)
  - Huggingface 다운로드 느릴 수 있음
- **추정 메모리**: ~0.8-1GB
- **추천도**: ⭐⭐⭐ (좋지만, ko-sbert보다는 낮음)

### 4. DistilBERT + 한국어 Fine-tuning
- **장점**:
  - 매우 경량 (~268MB)
  - 배포 최적화됨
- **단점**:
  - 한국어 성능 감소
  - 법률 텍스트 성능 검증 필요
- **추정 메모리**: ~0.5-0.8GB
- **추천도**: ⭐⭐ (마지막 수단)

---

## 최종 결론

### **선택: `sentence-transformers/ko-sbert-multitask`**

**이유:**
1. 한국어 법률/규정 텍스트에 최적화
2. 메모리 효율: 1GB 서버에서 실행 가능
3. 커뮤니티 지원 활발
4. 속도 (inference): 신속

**1GB 서버에서의 동작 예상:**
- 벡터 임베딩 로드: ~1GB
- LLM (Gemini API 콜): 로컬 메모리 불필요 (API 사용)
- FastAPI 서버: ~0.2GB
- **총합: ~1.2GB (약간 여유 필요)**

→ **대체 안: 질문 임베딩만 로컬에서 처리, 질문(5-10 토큰)만 벡터화**
→ 이 경우 메모리 훨씬 절감 가능

---

## 구현 계획

### Phase 1: 로컬 PC에서 테스트 (100개 조문)
1. `ko-sbert-multitask` 다운로드 및 테스트
2. 100개 조문 임베딩 생성 (~1-2분)
3. 벡터를 SQLite에 저장 (BLOB으로)
4. RRF 검색 로직 구현

### Phase 2: 검증 (상장폐지 사유 테스트)
1. `상장폐지 사유` 질문 임베딩
2. Hybrid 검색 수행
3. FTS5만 vs Hybrid 결과 비교

### Phase 3: 전체 확대 (10,472개)
1. 배치 처리로 전체 임베딩 (메모리 안정성 체크)
2. 1GB 서버 호환성 테스트 (배포 전 필수)

---

## 의존성 추가

```bash
pip install sentence-transformers
pip install sqlite-vec  # 또는 numpy + 직접 구현
```

선택 이유:
- `sentence-transformers`: 가장 안정적
- `sqlite-vec` vs `numpy`: 
  - sqlite-vec 사용 (SQLite 네이티브 지원, 속도 우수)
  - 또는 numpy로 유사도 계산 (더 가벼움)
